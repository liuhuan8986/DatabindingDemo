<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    >
    <data>
        <variable
            name="user"
            type="com.liuhuan.databindingdemo.entity.User"/>
        <variable
            name="userEvent"
            type="com.liuhuan.databindingdemo.MainActivity"/>
        <variable
            name="position"
            type="int"/>
    </data>
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{() -> userEvent.onClickUser(user)}"
        >
        <TextView
            android:id="@+id/userName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{user.name}"
            android:layout_marginBottom="8dp"
            />
        <TextView
            android:id="@+id/userAge"
            android:layout_below="@id/userName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:text="@{String.valueOf(user.age)}"
            />
        <TextView
            android:id="@+id/userSex"
            android:layout_below="@id/userName"
            android:layout_alignParentRight="true"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{user.sex}"
            android:onClick="@{()->userEvent.onClickPosition(position)}"
            />
        <View
            android:layout_below="@id/userSex"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="8dp"
            android:background="@color/colorAccent"/>
    </RelativeLayout>
</layout>
